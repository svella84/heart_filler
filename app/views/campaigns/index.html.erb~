<div class="row">

<h1>Listing campaigns</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th>User</th>
      <th>Category</th>
      <th>Title</th>
      <th>Description</th>
      <th>Target</th>
      <th>Budget</th>
      <th>Expiration</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>
	<td><%= image_tag campaign.image_url.url(:thumb) %></td>
        <td><%= campaign.user_id %></td>
	<td><%= campaign.category.name %></td>
        <td><%= campaign.title %></td>
        <td><%= campaign.description %></td>
        <td><%= number_to_currency(campaign.target, unit: "€", separator: ",", delimiter: ".", format: "%n %u") %></td>
        <td><%= number_to_currency(campaign.budget, unit: "€", separator: ",", delimiter: ".", format: "%n %u") %></td>
        <td><%= campaign.expiration %></td>
        <td><%= link_to 'Show', campaign, class: "tiny button"%></td>
        <td><%= link_to 'Edit', edit_campaign_path(campaign), class: "tiny button" %></td>
        <td><%= link_to 'Destroy', campaign, method: :delete, data: { confirm: 'Sei sicuro di voler eliminare questa campagna?' }, class: "tiny button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Campaign', new_campaign_path %>

</div>
