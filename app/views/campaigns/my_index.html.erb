<h1>Le Mie Campagne</h1>

<%= will_paginate @campaigns %>

<ul>
  <% @campaigns.each do |campaign| %>
    <li>
      <%= link_to campaign.title, campaign_path(campaign) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Crea Campagna', new_campaign_path, class: 'btn btn-large btn-info' %>